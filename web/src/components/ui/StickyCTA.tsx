/**
 * StickyCTA is intentionally rendered as a no-op for v1 of this brand.
 * Sticky-bottom CTAs work well for niche-specific lead-gen but conflict
 * with the editorial, typographic-first design language we want here.
 * Reinstate if launch data shows it would meaningfully help conversion.
 */
export function StickyCTA() {
  return null;
}
